import React from 'react';

function TransactionsStatistics({ statistics }) {
    return (
        <div>
            {/* Display total sale amount, total sold items, and total not sold items */}
        </div>
    );
}

export default TransactionsStatistics;